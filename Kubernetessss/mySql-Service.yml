apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: BankApp-namespace
  labels:
    app: mysql-service
spec:
  selector:
    app: MySql-deployment
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
  type: ClusterIP
